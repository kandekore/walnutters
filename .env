# --- Database ---
DATABASE_URL="file:./dev.db"

# --- Auth.js (NextAuth v5) ---
# Generate a real secret for production:  openssl rand -base64 32
AUTH_SECRET="walnutterz-dev-secret-change-me-in-production-please-0000"
AUTH_TRUST_HOST="true"

# --- Site ---
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# --- Stripe (optional) ---
# Leave blank to run in safe MOCK payment mode (no real charges, no keys needed).
# Add your test keys to enable real Stripe flows.
STRIPE_SECRET_KEY=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""
STRIPE_WEBHOOK_SECRET=""
